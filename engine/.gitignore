# Compiled binary (build artifact from `go build` / `make release` / `make install`)
/unravel
/unravel-fresh
# Legacy / ad-hoc build outputs
/engine
/engine-fresh
# Build-time embedded Gemini API key (also matched by the root *.key rule).
# Never commit this; it is baked into the binary via -ldflags at build time.
/gemini.key
